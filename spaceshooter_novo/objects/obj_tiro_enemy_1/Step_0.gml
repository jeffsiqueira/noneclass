/// @description voltando a scale do tiro

// setamos o tamanho configurado no Create Event e configuramos para retornar ao tamanho original
image_xscale = lerp(image_xscale, 1, .3);
image_yscale = lerp(image_xscale, 1, .3);